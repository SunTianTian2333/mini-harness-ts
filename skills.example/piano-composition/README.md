# piano-composition skill

Agent 侧钢琴作曲 skill（A1）。首次使用或更新后，复制到运行时目录：

```bash
cp -r skills.example/piano-composition .mini-harness/skills/
```

然后重启 `npm run dev`，system prompt catalog 会出现 `piano-composition`。
